// Usecase removido do fluxo do Budget Sales App.
// O sistema local não expõe endpoint de recuperação de senha.
// Arquivo mantido apenas para compatibilidade de compilação.

/// Stub – não utilizado no fluxo atual.
class RecoveryPassword {
  const RecoveryPassword();
}

/// Stub de parâmetros – não utilizado no fluxo atual.
class ParamsRecovery {
  const ParamsRecovery();
}
