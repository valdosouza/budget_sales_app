// Página removida do fluxo do Budget Sales App.
// O sistema local não utiliza troca de senha via app.
// Mantida apenas para compatibilidade de compilação com testes legados.
import 'package:flutter/material.dart';

class AuthChangePasswordPage extends StatelessWidget {
  const AuthChangePasswordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Funcionalidade não disponível.')),
    );
  }
}
